import React from 'react'
import Title from './Title'

const ComponentChange = (props) => {
  return (
    <div className="component-change-index">
        <Title title={props.title}></Title>
    </div>
  )
}

export default ComponentChange